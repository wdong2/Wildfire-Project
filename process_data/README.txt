var_f_[estimator][index]: the variance for the estimator; index is the critical level distribution id (but they should be the same, error checking)
F_0_1_[estimator][index]: contains two list: [f(a,0), f(a,1)]; format of the two list: f(a,[])[l][t] (again the index does not metter)
worst_p_star_list: the worst p.